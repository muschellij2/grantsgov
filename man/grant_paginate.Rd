% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{grant_paginate}
\alias{grant_paginate}
\title{Paginate through an endpoint}
\usage{
grant_paginate(
  .f,
  ...,
  page_size = 5000,
  page_offset = 1,
  sort_order = NULL,
  max_pages = Inf,
  data_field = "data"
)
}
\arguments{
\item{.f}{Endpoint function, or a string naming one.}

\item{...}{Arguments passed to \code{.f}.}

\item{page_size}{Number of records per page.}

\item{page_offset}{First page offset.}

\item{sort_order}{Optional sort specification passed to \code{\link[=grant_pagination]{grant_pagination()}}.}

\item{max_pages}{Maximum number of pages to request. Use \code{Inf} to continue
until API pagination metadata or a short page indicates completion.}

\item{data_field}{Response field containing page records.}
}
\value{
A list of records with \code{pages} and \code{pagination_info} attributes.
}
\description{
Generic paginator for endpoint wrappers that accept a \code{pagination} argument
and return a \code{data} element plus optional \code{pagination_info} metadata. This
applies to \code{\link[=grant_search_opportunities]{grant_search_opportunities()}} and \code{\link[=grant_list_extracts]{grant_list_extracts()}}.
}
\examples{
mock_endpoint <- function(pagination) {
  list(
    data = list(list(page = pagination$page_offset)),
    pagination_info = list(
      page_offset = pagination$page_offset,
      page_size = pagination$page_size,
      total_pages = 2
    )
  )
}
grant_paginate(mock_endpoint, page_size = 2)

if (nzchar(Sys.getenv("GRANTS_GOV_API_KEY")) &&
    identical(tolower(Sys.getenv("GRANTSGOV_EXAMPLES_LIVE")), "true")) {
  grant_paginate(
    grant_list_extracts,
    page_size = 5000,
    sort_order = grant_sort("created_at"),
    max_pages = 1
  )
}
}
