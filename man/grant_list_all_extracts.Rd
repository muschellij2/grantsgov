% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{grant_list_all_extracts}
\alias{grant_list_all_extracts}
\title{Paginate through all extract metadata}
\usage{
grant_list_all_extracts(
  filters = list(),
  page_size = 5000,
  page_offset = 1,
  sort_order = grant_sort("created_at"),
  max_pages = Inf,
  api_key = grant_api_key(),
  base_url = grant_base_url()
)
}
\arguments{
\item{filters}{Named list of extract filters.}

\item{page_size}{Number of records per page.}

\item{page_offset}{First page offset.}

\item{sort_order}{Sort specification. The extracts endpoint requires a sort
order, so this defaults to \code{created_at} descending.}

\item{max_pages}{Maximum number of pages to request.}

\item{api_key}{API key. Defaults to \code{GRANTS_GOV_API_KEY}.}

\item{base_url}{API base URL.}
}
\value{
A list of extract metadata records.
}
\description{
Convenience wrapper around \code{grant_paginate()} for \code{\link[=grant_list_extracts]{grant_list_extracts()}}.
}
\examples{
if (nzchar(Sys.getenv("GRANTS_GOV_API_KEY")) &&
    identical(tolower(Sys.getenv("GRANTSGOV_EXAMPLES_LIVE")), "true")) {
  extracts <- grant_list_all_extracts(
    filters = list(extract_type = "opportunities_csv"),
    page_size = 5000
  )
  length(extracts)
}
}
