% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{grant_list_extracts}
\alias{grant_list_extracts}
\title{List extract metadata}
\usage{
grant_list_extracts(
  filters = list(),
  pagination = grant_pagination(sort_order = grant_sort("created_at")),
  api_key = grant_api_key(),
  base_url = grant_base_url()
)
}
\arguments{
\item{filters}{Named list of filters. Documented filters are \code{extract_type}
as a scalar value, such as \code{"opportunities_json"}, and \code{created_at} as a
date range from \code{\link[=grant_filter_date_range]{grant_filter_date_range()}}.}

\item{pagination}{Pagination list. Defaults to page 1 with 25 rows.}

\item{api_key}{API key. Defaults to \code{GRANTS_GOV_API_KEY}.}

\item{base_url}{API base URL.}
}
\value{
Parsed JSON as a list.
}
\description{
Maps to \code{POST /v1/extracts}.
}
\examples{
if (nzchar(Sys.getenv("GRANTS_GOV_API_KEY")) &&
    identical(tolower(Sys.getenv("GRANTSGOV_EXAMPLES_LIVE")), "true")) {
  extracts <- grant_list_extracts(
    filters = list(extract_type = "opportunities_csv"),
    pagination = grant_pagination(
      page_size = 5000,
      sort_order = grant_sort("created_at")
    )
  )
  length(extracts$data)
}
}
