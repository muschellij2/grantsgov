% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{grant_search_agencies}
\alias{grant_search_agencies}
\title{Search agencies}
\usage{
grant_search_agencies(
  query = NULL,
  query_operator = c("AND", "OR"),
  filters = list(),
  pagination = grant_pagination(sort_order = grant_sort("agency_name", "ascending")),
  api_key = grant_api_key(),
  base_url = grant_base_url()
)
}
\arguments{
\item{query}{Optional free-text query.}

\item{query_operator}{Query operator, \code{"AND"} or \code{"OR"}.}

\item{filters}{Named list of agency filters. Documented filters include
\code{has_active_opportunity}, \code{is_test_agency}, and \code{opportunity_statuses}.}

\item{pagination}{Pagination list. Sort fields are \code{agency_code} and
\code{agency_name}.}

\item{api_key}{API key. Defaults to \code{GRANTS_GOV_API_KEY}.}

\item{base_url}{API base URL.}
}
\value{
Parsed JSON as a list.
}
\description{
Maps to \code{POST /v1/agencies/search}.
}
\examples{
if (nzchar(Sys.getenv("GRANTS_GOV_API_KEY")) &&
    identical(tolower(Sys.getenv("GRANTSGOV_EXAMPLES_LIVE")), "true")) {
  agencies <- grant_search_agencies(
    query = "health",
    pagination = grant_pagination(
      page_size = 25,
      sort_order = grant_sort("agency_name", "ascending")
    )
  )
  length(agencies$data)
}
}
