% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{grant_recent_opportunities}
\alias{grant_recent_opportunities}
\title{Get recently posted opportunities}
\usage{
grant_recent_opportunities(
  period = c("day", "week"),
  days = NULL,
  end_date = Sys.Date(),
  query = NULL,
  query_operator = c("AND", "OR"),
  filters = list(),
  page_size = 5000,
  page_offset = 1,
  sort_order = grant_sort("post_date"),
  max_pages = Inf,
  api_key = grant_api_key(),
  base_url = grant_base_url()
)
}
\arguments{
\item{period}{Recent window, either \code{"day"} or \code{"week"}.}

\item{days}{Optional custom number of days to look back. If supplied, this
overrides \code{period}.}

\item{end_date}{Last date in the date window. Defaults to today.}

\item{query}{Optional free-text query.}

\item{query_operator}{Query operator, \code{"AND"} or \code{"OR"}.}

\item{filters}{Additional search filters. Do not include \code{post_date}; this
function sets it from \code{period}, \code{days}, and \code{end_date}.}

\item{page_size}{Number of records per page.}

\item{page_offset}{First page offset.}

\item{sort_order}{Sort specification. Defaults to \code{post_date} descending.}

\item{max_pages}{Maximum number of pages to request.}

\item{api_key}{API key. Defaults to \code{GRANTS_GOV_API_KEY}.}

\item{base_url}{API base URL.}
}
\value{
A list of opportunity search records.
}
\description{
Convenience wrapper for getting all opportunities posted in the last day or
last week. The Simpler Grants.gov opportunity search endpoint exposes this
as the \code{post_date} filter, so "created" opportunities are interpreted as
opportunities posted within the requested date window.
}
\examples{
if (nzchar(Sys.getenv("GRANTS_GOV_API_KEY")) &&
    identical(tolower(Sys.getenv("GRANTSGOV_EXAMPLES_LIVE")), "true")) {
  last_day <- grant_recent_opportunities("day", max_pages = 1)
  last_week <- grant_recent_opportunities("week", max_pages = 1)
  length(last_day)
  length(last_week)
}
}
