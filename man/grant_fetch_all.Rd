% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{grant_fetch_all}
\alias{grant_fetch_all}
\title{Fetch all records from a first-page response}
\usage{
grant_fetch_all(
  x,
  page_size = NULL,
  max_pages = Inf,
  api_key = grant_api_key(),
  base_url = NULL,
  data_field = NULL
)
}
\arguments{
\item{x}{A response object returned by a paginated grantsgov endpoint.}

\item{page_size}{Optional page size for the full collection. If \code{NULL}, the
function continues from \code{x} using the original page size. If different from
the original page size, the first page is requested again to avoid skipping
records.}

\item{max_pages}{Maximum total number of pages to include.}

\item{api_key}{API key. Defaults to \code{GRANTS_GOV_API_KEY}.}

\item{base_url}{Optional API base URL override. Defaults to the URL used by
the original response.}

\item{data_field}{Response field containing page records. Defaults to the
field recorded by the original endpoint, usually \code{"data"}.}
}
\value{
A list of records with \code{pages} and \code{pagination_info} attributes.
}
\description{
Takes the output from a paginated endpoint call, such as
\code{\link[=grant_search_opportunities]{grant_search_opportunities()}}, and requests the remaining pages using the
same query, filters, and sort options. This is a convenience wrapper for the
common workflow where you inspect the first page and then decide to collect
all matching records.
}
\examples{
if (nzchar(Sys.getenv("GRANTS_GOV_API_KEY")) &&
    identical(tolower(Sys.getenv("GRANTSGOV_EXAMPLES_LIVE")), "true")) {
  first_page <- grant_search_opportunities(
    query = "cancer",
    filters = list(
      top_level_agency = grant_filter_one_of("HHS"),
      opportunity_status = grant_filter_one_of(c("posted", "forecasted"))
    ),
    pagination = grant_pagination(
      page_size = 25,
      sort_order = grant_sort("close_date", "ascending")
    )
  )
  all_records <- grant_fetch_all(first_page)
  length(all_records)
}
}
