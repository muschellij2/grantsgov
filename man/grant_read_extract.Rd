% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{grant_read_extract}
\alias{grant_read_extract}
\title{Download and read a CSV extract}
\usage{
grant_read_extract(
  extract,
  path = tempfile(fileext = ".csv"),
  overwrite = FALSE,
  expected_file_size = NULL,
  ...
)
}
\arguments{
\item{extract}{Metadata list containing a \code{download_path} or \code{download_url},
or a scalar URL.}

\item{path}{Local output path. Defaults to a temporary \code{.csv} file.}

\item{overwrite}{If \code{FALSE}, error when \code{path} already exists.}

\item{expected_file_size}{Optional expected downloaded file size in bytes.}

\item{...}{Additional arguments passed to \code{\link[readr:read_csv]{readr::read_csv()}}.}
}
\value{
A tibble with the downloaded file path in \code{attr(x, "file")}.
}
\description{
Downloads an extract with \code{\link[=grant_download_extract]{grant_download_extract()}} and reads it with
\code{\link[readr:read_csv]{readr::read_csv()}}. If readr reports parsing problems, this function emits a
warning. The downloaded file path is stored in the returned data frame's
\code{file} attribute.
}
\examples{
if (nzchar(Sys.getenv("GRANTS_GOV_API_KEY")) &&
    identical(tolower(Sys.getenv("GRANTSGOV_EXAMPLES_LIVE")), "true")) {
  extracts <- grant_list_extracts(
    filters = list(extract_type = "opportunities_csv"),
    pagination = grant_pagination(page_size = 1, sort_order = grant_sort("created_at"))
  )
  data <- grant_read_extract(
    extracts$data[[1]],
    expected_file_size = extracts$data[[1]]$file_size_bytes
  )
  attr(data, "file")
}
}
